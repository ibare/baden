// Schema is initialized in connection.ts to avoid module loading order issues.
// This file is kept for reference and potential future migrations.
export function initializeDatabase(): void {
  // Schema already initialized in connection.ts
}
